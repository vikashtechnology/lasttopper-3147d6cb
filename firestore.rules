rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser and native clients never access Firestore directly. All
    // application data flows through authenticated Vercel server routes using
    // Firebase Admin, which is not governed by client security rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
